# TODO

I would like to see a full UI rewrite, maybe use shadcn (might be overkill).

The clouds could use improvement.

Sun needs to be set in the skybox, not fixed to the viewport.

Low priority:

Might be able to use grass models.

Maybe implement textures, my previous attempts were not satisfactory.

The original code has no seperation of concerns, it runs in codepen readily without the need of a server. Newer version might need cleaner seperation using a fullstack framework like tanstack, vue or react (single file components.)

Eventually the project might need to be renamed (it has nothing to do with Minecraft) but it helps get the project seen.